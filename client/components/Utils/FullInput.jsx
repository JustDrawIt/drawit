import styled from '@emotion/styled';

const FullInput = styled('input')`
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  cursor: pointer;
`;

export default FullInput;
