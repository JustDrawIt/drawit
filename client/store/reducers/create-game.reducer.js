import CreateGameActions from '../actions/create-game.actions';

const createGameDefaultState = {

};

export default function createGameReducer(state = createGameDefaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
